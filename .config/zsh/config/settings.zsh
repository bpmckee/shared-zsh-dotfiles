# Step C-s and C-q from freezing the output.
setopt noflowcontrol

# Allow command substitution in the prompt (and param/arithmetic expansion).
setopt prompt_subst

# Save each command timestamp + duration in the history file.
setopt extended_history

# Report the status of background jobs immediately.
setopt notify

# Allow comments in interactive code.
setopt interactivecomments

